# 安装
```shell
npm install i3v-file-attribute -S
```

# 快速开始
```javascript
import Vue from 'vue'
import i3vFileAttribute from 'i3v-file-attribute'

Vue.use(i3vFileAttribute)

// or
import {
  FileAttribute,
  // ...
} from 'i3v-file-attribute'

Vue.component(FileAttribute.name, FileAttribute)
```
